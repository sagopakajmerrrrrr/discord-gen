from flask import Flask
from threading import Thread

app = Flask('')


@app.route('/')
val = lambda: 'Bot aktif!'


def run():
  app.run(host='0.0.0.0', port=8080)


def keep_alive():
  t = Thread(target=run)
  t.start()


# Botu başlatmadan önce web sunucusunu çalıştırıyoruz
keep_alive()
